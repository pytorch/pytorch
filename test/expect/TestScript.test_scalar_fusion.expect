graph(%x : Float()
      %y : Float()) {
  %2 : Float() = prim::FusionGroup_0[device=-1](%y, %x)
  return (%2);
}
with prim::FusionGroup_0 = graph(%0 : Float()
      %1 : Float()) {
  %3 : Float() = aten::mul(%1, 2)
  %5 : Float() = aten::add(%3, %0, 1)
  return (%5);
}
