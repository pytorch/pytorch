graph(%x : Float(*, *)) {
  %1 : Float(*, *) = prim::FusionGroup_0[device=0](%x)
  return (%1);
}
with prim::FusionGroup_0 = graph(%0 : Float(*, *)) {
  %y : Float(*, *) = aten::add(%0, 3, 1)
  %4 : Float(*, *) = aten::mul(%0, %y)
  return (%4);
}
