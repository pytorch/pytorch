graph(%x : Double(*, *)) {
  %c : Dynamic[] = prim::If(True)
    block0() {
      %c.1 : Dynamic[] = prim::ListConstruct(%x, %x)
      -> (%c.1)
    }
    block1() {
      %c.2 : Dynamic[] = prim::ListConstruct(%x, %x, %x)
      -> (%c.2)
    }
  %6 : Dynamic = aten::cat(%c, 0)
  return (%6);
}
