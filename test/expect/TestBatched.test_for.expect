graph(%x.1_data : Dynamic
      %x.1_mask : Dynamic
      %x.1_dims : Dynamic
      %y_data : Dynamic
      %y_mask : Dynamic
      %y_dims : Dynamic) {
  %6 : int = prim::Constant[value={10}]()
  %7 : Byte() = prim::Constant[value={1}]()
  %x : Dynamic, %21 : Dynamic, %22 : Dynamic = prim::Loop(%6, %7, %x.1_data, %x.1_mask, %x.1_dims)
    block0(%_i_data : Dynamic, %5_data : Dynamic, %5_mask : Dynamic, %5_dims : Dynamic) {
      %15 : Long() = prim::Constant[value={1}]()
      %16 : Number = prim::TensorToNum(%15)
      %data : Dynamic = aten::add(%5_data, %y_data, %16)
      %mask : Dynamic = aten::mul(%5_mask, %y_mask)
      %dims : Dynamic = aten::__or__(%5_dims, %y_dims)
      %20 : Byte() = prim::Constant[value={1}]()
      -> (%20, %data, %mask, %dims)
    }
  return (%x, %21, %22);
}
