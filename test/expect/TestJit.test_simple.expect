graph(%1 : (1), %2 : (1)) {
  %15.0 : (1) = fusion_group_0(%1, %2), uses = [[%0.i0]];
  return (%15.0);
}
with fusion_group_0 = graph(%5 : (1), %8 : (1)) {
  %9 : (1) = Add(%5, %8), uses = [%7.i1];
  %7 : (1) = Mul(%5, %9), uses = [%4.i0];
  %4 : (1) = Tanh(%7), uses = [%2.i0];
  %2 : (1) = Sigmoid(%4), uses = [%0.i0];
  return (%2);
}
