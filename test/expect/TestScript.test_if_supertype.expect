graph(%y_1 : Float(*, *)
      %z_2 : Long(*, *)
      %z_1 : Float(*, *)) {
  %3 : bool = prim::Constant[value=1]()
  %x : Float(*, *), %y : Dynamic, %z : Dynamic = prim::If(%3)
    block0() {
      -> (%y_1, %z_2, %z_1)
    }
    block1() {
      -> (%y_1, %y_1, %z_2)
    }
  return (%x, %y, %z);
}
