graph(%x : Double(3, 1, 5)) {
  %1 : int = prim::Constant[value=2]()
  %2 : int = prim::Constant[value=1]()
  %3 : int = prim::Constant[value=0]()
  %B : int = aten::size(%x, %3)
  %C : int = aten::size(%x, %2)
  %T : int = aten::size(%x, %1)
  %7 : int[] = prim::ListConstruct(%T, %B, %C)
  %8 : Double(*, *, *) = aten::view(%x, %7)
  return (%8);
}
