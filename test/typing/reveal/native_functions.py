# mypy: disable-error-code="possibly-undefined"
# flake8: noqa
# This file is generated by tools/typing/gen_native_function_type_tests.py.
# Do not edit manually; update TEST_CASES in that generator.
import torch


x = torch.randn(2, 3)
matrix = torch.randn(2, 3)
matrix_t = torch.randn(3, 2)
cube = torch.randn(2, 3, 4)
indices = torch.tensor([0, 1], dtype=torch.long)

reveal_type(torch.relu(x))  # E: {Tensor}
reveal_type(torch.abs(x))  # E: {Tensor}
reveal_type(torch.clamp(x, min=0.0, max=1.0))  # E: {Tensor}
reveal_type(torch.matmul(matrix, matrix_t))  # E: {Tensor}
reveal_type(torch.index_select(matrix, 0, indices))  # E: {Tensor}
reveal_type(torch.flatten(cube, start_dim=1))  # E: {Tensor}
reveal_type(torch.reshape(x, (3, 2)))  # E: {Tensor}
reveal_type(torch.permute(cube, (2, 0, 1)))  # E: {Tensor}
reveal_type(torch.transpose(matrix, 0, 1))  # E: {Tensor}
reveal_type(torch.sum(matrix, dim=1, keepdim=True, dtype=torch.float64))  # E: {Tensor}
reveal_type(torch.amax(cube, dim=(1, 2), keepdim=True))  # E: {Tensor}
reveal_type(torch.empty((2, 3), dtype=torch.float32, device=None))  # E: {Tensor}
reveal_type(torch.zeros(2, 3, out=torch.empty(2, 3)))  # E: {Tensor}
