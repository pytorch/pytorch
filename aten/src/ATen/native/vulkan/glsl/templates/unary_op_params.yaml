unary_op:
  parameter_names_with_default_values:
    NAME: exp
    OPERATOR: exp(X)
  parameter_values:
    - NAME: sqrt
      OPERATOR: sqrt(X)
    - NAME: log
      OPERATOR: log(X)

unary_op_inplace:
  parameter_names_with_default_values:
    NAME: exp_
    OPERATOR: exp(X)
  parameter_values:
    - NAME: sqrt_
      OPERATOR: sqrt(X)
    - NAME: log_
      OPERATOR: log(X)
