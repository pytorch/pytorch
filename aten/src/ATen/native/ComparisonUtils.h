#include <ATen/core/TensorBase.h>
#include <algorithm>
#include <vector>

namespace at { namespace native {

inline void _assert_true(bool value) {
  AT_ASSERT(value);
}

inline void _assert_true(bool value) {
  AT_ASSERT(value);
}


}}  // namespace at::native
