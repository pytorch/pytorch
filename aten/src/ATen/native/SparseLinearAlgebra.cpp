namespace at {
namespace native {
  Tensor sparse_gcs_mm_cpu(const Tensor& sparse_, Tensor& result, const Tensor& t, const Tensor& dense,
                         const Scalar& alpha, const Scalar& beta) {

    
  }

}}
