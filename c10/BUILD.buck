load(":build.bzl", "c10_buck_target")
load("//tools/build_defs:glob_defs.bzl", "subdir_glob")

c10_buck_target(
    name = "c10",
    glob = glob,
    rule_name = cxx_library,
    subdir_glob = subdir_glob,
    deps = [
        "//third_party:fmt",
        "//third_party:glog",
    ],
)
