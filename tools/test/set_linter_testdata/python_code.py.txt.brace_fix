from torch.utils._ordered_set import OrderedSet
ignored = set()  # noqa: set_linter
a = set()
b = 'set()'
c = set
ignored = set()  # noqa: set_linter
d = c.set
f = (
   set(
   )
)
ignored = (
   set(  # noqa: set_linter
   )
)
e = """ set()
set() set x.set set()
\""""

# From here, every mention of set is ignored

class A:
    def set(self, x):
        self.x = x

set = A().set  # Also gets ignored!

good = {}
replace1 = OrderedSet([1])
replace2 = OrderedSet([1, 2])
replace3 = {'a': OrderedSet([2, 3]), 'b': OrderedSet([i for i in range(3)])}
