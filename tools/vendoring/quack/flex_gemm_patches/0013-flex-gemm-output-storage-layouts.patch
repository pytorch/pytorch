diff --git a/epi_ops.py b/epi_ops.py
index 2bfa2b0..1c46e18 100644
--- a/epi_ops.py
+++ b/epi_ops.py
@@ -1192,0 +1193 @@ class _GroupedLocalReduceParams(NamedTuple):
+    logical_n: object
@@ -1239 +1240 @@ class GroupedLocalReduce(VecReduce):
-    """Store or broadcast generated grouped local-reduce values."""
+    """Store or broadcast grouped reductions using the main output's M/N bounds."""
@@ -1252,0 +1254,6 @@ class GroupedLocalReduce(VecReduce):
+        tensor = getattr(args, self.name)
+        reference_output = (
+            args.mAuxOut[0] if args.tensor_epilogue_returns_aux else args.mAuxOut
+        )
+        if tensor is not None and args.local_reduce_output_layout is not None:
+            tensor = args.local_reduce_output_layout(tensor)
@@ -1255 +1262 @@ class GroupedLocalReduce(VecReduce):
-                getattr(args, self.name),
+                tensor,
@@ -1258,0 +1266,2 @@ class GroupedLocalReduce(VecReduce):
+                cute.size(reference_output, mode=[1])
+                * gemm.grouped_n_contract_group,
@@ -1365,3 +1374,5 @@ class GroupedLocalReduce(VecReduce):
-            limit_m = min(
-                varlen_manager.len_m(batch_idx) - tile_coord_mnkl[0] * tile_M,
-                tile_M,
+            logical_m = varlen_manager.len_m(batch_idx)
+            limit_m = min(logical_m - tile_coord_mnkl[0] * tile_M, tile_M)
+            limit_n = min(
+                param.logical_n - tile_coord_mnkl[1] * tile_N,
+                tile_N,
@@ -1369 +1380,4 @@ class GroupedLocalReduce(VecReduce):
-            limit_n = min(cute.size(param_tensor, mode=[2]) - tile_coord_mnkl[1] * tile_N, tile_N)
+            if const_expr(axis == 1):
+                limit_groups = param.logical_n // group
+            else:
+                limit_groups = logical_m // group
@@ -1379,5 +1392,0 @@ class GroupedLocalReduce(VecReduce):
-                    limit_groups = (
-                        param_tensor.shape[2]
-                        if not varlen_manager.varlen_m
-                        else param_tensor.shape[1]
-                    )
@@ -1466,5 +1474,0 @@ class GroupedLocalReduce(VecReduce):
-                    limit_groups = (
-                        param_tensor.shape[1]
-                        if not varlen_manager.varlen_m
-                        else param_tensor.shape[0]
-                    )
@@ -1518 +1521,0 @@ class GroupedLocalReduce(VecReduce):
-                limit_groups = param_tensor.shape[2] if not varlen_manager.varlen_m else param_tensor.shape[1]
@@ -1522 +1524,0 @@ class GroupedLocalReduce(VecReduce):
-                limit_groups = param_tensor.shape[1] if not varlen_manager.varlen_m else param_tensor.shape[0]
@@ -1536,0 +1539 @@ class GroupedLocalReduce(VecReduce):
+                and param_tensor.stride[2] == 1
diff --git a/gemm_act.py b/gemm_act.py
index 6ed6e39..b2a98a4 100644
--- a/gemm_act.py
+++ b/gemm_act.py
@@ -62,0 +63,8 @@ from .rounding import RoundingMode, convert_f32_to_bf16_sr, epilogue_aux_out_sr_
+class OutputLayoutSpec(NamedTuple):
+    """CuTe view and physical carrier contract registered under a stable key."""
+
+    tensor_fn: Callable
+    fake_shape_fn: Callable
+    carrier_ndim: int
+
+
@@ -65,0 +74,17 @@ _local_reduce_finalize_fns: dict[str, Callable] = {}
+_output_layout_specs: dict[str, OutputLayoutSpec] = {}
+
+
+def register_output_layout(
+    key: str, tensor_fn: Callable, fake_shape_fn: Callable, carrier_ndim: int
+) -> None:
+    """Register a cache-stable physical output-layout implementation.
+
+    Args:
+        key: Stable identifier included in the compiled-kernel cache key.
+        tensor_fn: CuTeDSL callable mapping the physical carrier to its logical view.
+        fake_shape_fn: Callable producing the symbolic physical carrier shape.
+        carrier_ndim: Required rank of the runtime carrier tensor.
+    """
+    _output_layout_specs[key] = OutputLayoutSpec(
+        tensor_fn, fake_shape_fn, carrier_ndim
+    )
@@ -141,0 +167,15 @@ def validate_grouped_n_contract_device(
+def normalize_local_reduce_output_layout(
+    layout: str | None, has_local_reduce_output: bool
+) -> str | None:
+    """Validate the optional physical layout for the local-reduction store."""
+    if layout is None:
+        return None
+    if not has_local_reduce_output:
+        raise RuntimeError(
+            "local_reduce_output_layout requires a local_reduce_out tensor"
+        )
+    if layout not in _output_layout_specs:
+        raise NotImplementedError(f"unsupported output layout: {layout!r}")
+    return layout
+
+
@@ -178,0 +219 @@ class GemmActMixin(ComposableEpiMixin):
+        local_reduce_output_layout: cutlass.Constexpr[Optional[Callable]] = None
@@ -555,0 +597 @@ class GemmGroupedNContractMixin(GemmActMixin):
+        GroupedLocalReduce("mLocalReduce"),
@@ -582,0 +625 @@ class GemmGroupedNContract4Sm100(GemmGroupedNContractMixin, GemmSm100):
+        GroupedLocalReduce("mLocalReduce"),
@@ -751,0 +795 @@ def _compile_gemm_act(
+    local_reduce_output_layout,
@@ -866 +910,5 @@ def _compile_gemm_act(
-    if local_reduce_dtype is not None and local_reduce_axis == 0:
+    if local_reduce_output_layout is not None:
+        local_reduce_shape = _output_layout_specs[
+            local_reduce_output_layout
+        ].fake_shape_fn(l, m, n, local_reduce_axis, local_reduce_ndim)
+    elif local_reduce_dtype is not None and local_reduce_axis == 0:
@@ -882 +930 @@ def _compile_gemm_act(
-        local_reduce_leading_dim = 2 if local_reduce_ndim == 3 else 1
+        local_reduce_leading_dim = local_reduce_ndim - 1
@@ -926,0 +975,5 @@ def _compile_gemm_act(
+        local_reduce_output_layout=(
+            None
+            if local_reduce_output_layout is None
+            else _output_layout_specs[local_reduce_output_layout].tensor_fn
+        ),
@@ -1006,0 +1060 @@ def gemm_act(
+    local_reduce_output_layout: str | None = None,
@@ -1020 +1074,2 @@ def gemm_act(
-    ``local_reduce_out`` is always 3-D at this QuACK boundary.
+    ``local_reduce_out`` is normally 3-D; a registered output layout may use a
+    different physical carrier rank.
@@ -1034,0 +1090,4 @@ def gemm_act(
+    ``local_reduce_output_layout`` optionally selects a registered physical view
+    for ``local_reduce_out``. The registered callback is already specialized for
+    the physical GEMM orientation.
+
@@ -1071 +1130 @@ def gemm_act(
-        if tensor_epilogue_returns_aux or tensor_epilogue_returns_local_reduce:
+        if tensor_epilogue_returns_aux:
@@ -1073 +1132 @@ def gemm_act(
-                "grouped_n_contract does not compose with auxiliary outputs"
+                "grouped_n_contract does not compose with full-shape auxiliary outputs"
@@ -1202,0 +1262,3 @@ def gemm_act(
+    local_reduce_output_layout = normalize_local_reduce_output_layout(
+        local_reduce_output_layout, local_reduce_out is not None
+    )
@@ -1245,2 +1307,9 @@ def gemm_act(
-        if local_reduce_out.ndim != 3:
-            raise NotImplementedError("QUACK local_reduce_out must be 3-D")
+        expected_local_reduce_ndim = (
+            3
+            if local_reduce_output_layout is None
+            else _output_layout_specs[local_reduce_output_layout].carrier_ndim
+        )
+        if local_reduce_out.ndim != expected_local_reduce_ndim:
+            raise NotImplementedError(
+                f"QUACK local_reduce_out must be {expected_local_reduce_ndim}-D"
+            )
@@ -1254 +1323 @@ def gemm_act(
-        2 if local_reduce_out is not None and local_reduce_out.ndim == 3 else 1
+        local_reduce_out.ndim - 1 if local_reduce_out is not None else 1
@@ -1294,0 +1364 @@ def gemm_act(
+        local_reduce_output_layout,
@@ -1338,0 +1409 @@ def gemm_act(
+        local_reduce_output_layout=None,
