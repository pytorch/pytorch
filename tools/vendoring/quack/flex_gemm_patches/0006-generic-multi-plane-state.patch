diff --git a/quack/epilogue/frontend.py b/quack/epilogue/frontend.py
index 6d8b509013d62608ef71834e7e4ab74f7b5949b5..9a69602f7bdca595623467a8501d785e04788be1 100644
--- a/quack/epilogue/frontend.py
+++ b/quack/epilogue/frontend.py
@@ -61,2 +61,3 @@ guesses about vectorization.
-  ``outs={name: sink_op}`` is the general form for any sink op
-  (e.g. OnlineLSEReduce's coupled (max, sum) accumulator).
+  ``outs={name: sink_op}`` is the general form for any sink op. A sink declares
+  ``sink_arity``; arity greater than one means the fn returns a tuple of
+  independently collected value planes for the sink to combine.
@@ -354,4 +355,6 @@ class _FragmentEpiModMixin(_EpiModMixinBase):
-            value = result[name]
-            sink_tmp = cute.make_rmem_tensor(value.shape, value.element_type)
-            sink_tmp.store(value)
-            sink_tmps.append(sink_tmp)
+            planes = []
+            for value in self._value_planes(name, ops_by_name[name].sink_arity, result[name]):
+                plane = cute.make_rmem_tensor(value.shape, value.element_type)
+                plane.store(value)
+                planes.append(plane)
+            sink_tmps.append(tuple(planes))
@@ -450 +453,3 @@ class EpiMod:
-            if getattr(op, "scaled", False) and self.mode == "acc_pair":
+            if not isinstance(op.sink_arity, int) or op.sink_arity < 1:
+                raise ValueError(f"sink {name!r}: sink_arity must be a positive integer")
+            if op.sink_arity > 1 and self.mode == "acc_pair":
@@ -452 +457 @@ class EpiMod:
-                    f"sink {name!r}: scaled reduces are not supported in acc_pair mode yet "
+                    f"sink {name!r}: multi-plane sinks are not supported in acc_pair mode "
@@ -454,0 +460,8 @@ class EpiMod:
+        for name in self.prepass_outs:
+            op = self.ops.get(name)
+            if op is None:
+                op = self.sinks.get(name)
+            if op is not None and op.sink_arity > 1:
+                raise ValueError(
+                    f"sink {name!r}: multi-plane sinks are not supported in prepass_outs"
+                )
@@ -795 +808 @@ class EpiMod:
-                raise ValueError("swap_ab requires element mode and orientation-aware epilogue ops")
+                raise ValueError("swap_ab requires element-mode and orientation-aware epilogue ops")
diff --git a/quack/epilogue/ops.py b/quack/epilogue/ops.py
index b75771852f3ef41b23904725f6ff9d5ebd7d1acc..e6b441e5fc50e36cc545179d31c37c2658ac6c1b 100644
--- a/quack/epilogue/ops.py
+++ b/quack/epilogue/ops.py
@@ -18,2 +17,0 @@ import operator
-import hashlib
-import inspect
@@ -34,0 +33 @@ from quack.cute_dsl_utils import torch2cute_dtype_map
+from quack.gemm_runtime.identity import semantic_value_key
@@ -110,13 +109,2 @@ def _callable_config_key(fn):
-    """Stable, picklable identity for a callable stored in an EpiOp config."""
-    if fn is None:
-        return None
-    try:
-        source = inspect.getsource(fn).encode()
-    except (OSError, TypeError):
-        code = getattr(fn, "__code__", None)
-        source = code.co_code if code is not None else repr(fn).encode()
-    return (
-        getattr(fn, "__module__", ""),
-        getattr(fn, "__qualname__", repr(fn)),
-        hashlib.sha256(source).hexdigest(),
-    )
+    """Fail-closed semantic identity for a callable stored in an EpiOp config."""
+    return None if fn is None else semantic_value_key(fn, set(), force_source=True)
@@ -325,0 +314 @@ class EpiOp:
+    sink_arity = 1
@@ -335,4 +324,6 @@ class EpiOp:
-    def fn_sink_flush(self, gemm, state, frag):
-        """Fold a fragment of fn-produced values into this op's accumulator.
-        ``state`` is the begin_loop result; ``frag`` is elementwise-congruent
-        with the accumulator tile fragment."""
+    def fn_sink_flush(self, gemm, state, *fragments):
+        """Fold the declared sink value planes into this op's accumulator.
+
+        ``state`` is the begin_loop result. Each fragment is elementwise-congruent
+        with the accumulator tile and corresponds to one of ``sink_arity`` planes.
+        """
@@ -1579,0 +1571,5 @@ class VecReduce(EpiOp):
+    @property
+    def sink_arity(self):
+        """Number of value planes returned to this sink by the epilogue fn."""
+        return 2 if self.scaled else 1
+
diff --git a/quack/epilogue/visit.py b/quack/epilogue/visit.py
index 953ef2fdf6eeca121a74b8f081974200402cf75d..ede70e746aeee693f19f5981451866d0413d964e 100644
--- a/quack/epilogue/visit.py
+++ b/quack/epilogue/visit.py
@@ -55,0 +56,13 @@ class _EpiModMixinBase(ComposableEpiMixin):
+    def _value_planes(self, name, arity, value):
+        """Normalize one fn return to its declared tuple of value planes."""
+        if arity == 1:
+            return (value,)
+        assert not isinstance(value, (F2, F16Lanes, Pair)), (
+            f"output {name!r} returned one packed-lane value where {arity} "
+            "state planes are required"
+        )
+        assert isinstance(value, tuple) and len(value) == arity, (
+            f"output {name!r} must return {arity} value planes"
+        )
+        return value
+
@@ -57,2 +70 @@ class _EpiModMixinBase(ComposableEpiMixin):
-        """One collection fragment per sink op; scaled reduces get a
-        (val, scale) fragment pair so the fold can be a single fused FMA."""
+        """Allocate each sink's fixed tuple of collection fragments."""
@@ -60,7 +72,3 @@ class _EpiModMixinBase(ComposableEpiMixin):
-            (
-                (
-                    cute.make_rmem_tensor(shape, self.acc_dtype),
-                    cute.make_rmem_tensor(shape, self.acc_dtype),
-                )
-                if getattr(ops_by_name[s], "scaled", False)
-                else cute.make_rmem_tensor(shape, self.acc_dtype)
+            tuple(
+                cute.make_rmem_tensor(shape, self.acc_dtype)
+                for _ in range(ops_by_name[name].sink_arity)
@@ -68 +76 @@ class _EpiModMixinBase(ComposableEpiMixin):
-            for s in self._epi_mod_sinks
+            for name in self._epi_mod_sinks
@@ -73,7 +81,2 @@ class _EpiModMixinBase(ComposableEpiMixin):
-        for sname, stmp in zip(self._epi_mod_sinks, sink_tmps):
-            if const_expr(isinstance(stmp, tuple)):
-                ops_by_name[sname].fn_sink_flush(
-                    self, epi_loop_tensors[sname], stmp[0], scale=stmp[1]
-                )
-            else:
-                ops_by_name[sname].fn_sink_flush(self, epi_loop_tensors[sname], stmp)
+        for name, fragments in zip(self._epi_mod_sinks, sink_tmps):
+            ops_by_name[name].fn_sink_flush(self, epi_loop_tensors[name], *fragments)
@@ -220 +223 @@ class _EpiModMixinBase(ComposableEpiMixin):
-            val_frags = outs + sink_tmps
+            val_frags = tuple((out,) for out in outs) + sink_tmps
@@ -236,7 +239,4 @@ class _EpiModMixinBase(ComposableEpiMixin):
-                for vname, vfrag in zip(val_names, val_frags):
-                    if const_expr(isinstance(vfrag, tuple)):
-                        # Scaled sink: the fn returns the (val, scale) factors.
-                        v, s = res[vname]
-                        vfrag[0][i], vfrag[1][i] = v, s
-                    else:
-                        vfrag[i] = res[vname]
+                for vname, vfrags in zip(val_names, val_frags):
+                    values = self._value_planes(vname, len(vfrags), res[vname])
+                    for plane, value in zip(vfrags, values):
+                        plane[i] = value
@@ -259,2 +259,2 @@ class _EpiModMixinBase(ComposableEpiMixin):
-            # (Scaled sinks are rejected in acc_pair mode at EpiMod init: a
-            # tuple return already means the two lanes here.)
+            # Multi-plane sinks are rejected in acc_pair mode because a tuple
+            # return already means the two adjacent accumulator lanes here.
@@ -262 +262 @@ class _EpiModMixinBase(ComposableEpiMixin):
-                cute.make_rmem_tensor(tRS_rD.layout.shape, self.acc_dtype)
+                (cute.make_rmem_tensor(tRS_rD.layout.shape, self.acc_dtype),)
@@ -267 +267 @@ class _EpiModMixinBase(ComposableEpiMixin):
-                for p in (cute.flat_divide(t, cute.make_layout(2)) for t in sink_tmps)
+                for p in (cute.flat_divide(t[0], cute.make_layout(2)) for t in sink_tmps)
@@ -320,2 +320 @@ class _EpiModMixinBase(ComposableEpiMixin):
-            for sname, stmp in zip(self._epi_mod_sinks, sink_tmps):
-                ops_by_name[sname].fn_sink_flush(self, epi_loop_tensors[sname], stmp)
+            self._flush_sinks(ops_by_name, epi_loop_tensors, sink_tmps)
@@ -328,4 +327,3 @@ class _EpiModMixinBase(ComposableEpiMixin):
-        # Sink values are collected into a plain fragment per sink op (a
-        # (val, scale) fragment pair for scaled reduces), then handed to the
-        # op's fn_sink_flush (fragment-level: the op owns the fold into its —
-        # possibly aliased, possibly coupled — accumulators).
+        # Sink values are collected into one or more fragments per sink, then
+        # handed to fn_sink_flush (fragment-level: the op owns the fold into
+        # its possibly aliased or coupled accumulators).
@@ -335 +333 @@ class _EpiModMixinBase(ComposableEpiMixin):
-        val_frags = outs + sink_tmps
+        val_frags = tuple((out,) for out in outs) + sink_tmps
@@ -355,8 +353,7 @@ class _EpiModMixinBase(ComposableEpiMixin):
-                for vname, vfrag in zip(val_names, val_frags):
-                    if const_expr(isinstance(vfrag, tuple)):
-                        v, s = res[vname]
-                        vfrag[0][2 * i], vfrag[0][2 * i + 1] = v[0], v[1]
-                        vfrag[1][2 * i], vfrag[1][2 * i + 1] = s[0], s[1]
-                    else:
-                        v = res[vname]
-                        vfrag[2 * i], vfrag[2 * i + 1] = v[0], v[1]
+                for vname, vfrags in zip(val_names, val_frags):
+                    values = self._value_planes(vname, len(vfrags), res[vname])
+                    for plane, value in zip(vfrags, values):
+                        if const_expr(isinstance(value, tuple)):
+                            plane[2 * i], plane[2 * i + 1] = value[0], value[1]
+                        else:
+                            plane[2 * i], plane[2 * i + 1] = value, value
@@ -376,6 +373,4 @@ class _EpiModMixinBase(ComposableEpiMixin):
-                for vname, vfrag in zip(val_names, val_frags):
-                    if const_expr(isinstance(vfrag, tuple)):
-                        v, s = res[vname]
-                        vfrag[0][i], vfrag[1][i] = v, s
-                    else:
-                        vfrag[i] = res[vname]
+                for vname, vfrags in zip(val_names, val_frags):
+                    values = self._value_planes(vname, len(vfrags), res[vname])
+                    for plane, value in zip(vfrags, values):
+                        plane[i] = value
diff --git a/quack/gemm_runtime/autotune.py b/quack/gemm_runtime/autotune.py
index dcc396df34ad21e66a1b24c9c91ef3ed192cc2ec..c1bba53e13ce230c9650327e923f16cef2945704 100644
--- a/quack/gemm_runtime/autotune.py
+++ b/quack/gemm_runtime/autotune.py
@@ -253,5 +252,0 @@ def _prune_for_mod(mod, transform_a, configs, named_args, *, config_constraints=
-        if config_constraints:
-            raise ValueError(
-                "no supported GemmConfig matches "
-                f"config_constraints={dict(config_constraints)!r} for this call"
-            )
@@ -258,0 +254,3 @@ def _prune_for_mod(mod, transform_a, configs, named_args, *, config_constraints=
+        diagnostic_configs = (
+            tuple(_config_space(mod, A.device)) if config_constraints else raw_configs
+        )
@@ -263 +261 @@ def _prune_for_mod(mod, transform_a, configs, named_args, *, config_constraints=
-            and not any(supports(config) for config in raw_configs)
+            and not any(supports(config) for config in diagnostic_configs)
@@ -267 +265,7 @@ def _prune_for_mod(mod, transform_a, configs, named_args, *, config_constraints=
-                f"no supported GemmConfig: {unsupported_ops[0].config_support_error(raw_configs)}"
+                "no supported GemmConfig: "
+                f"{unsupported_ops[0].config_support_error(diagnostic_configs)}"
+            )
+        if config_constraints:
+            raise ValueError(
+                "no supported GemmConfig matches "
+                f"config_constraints={dict(config_constraints)!r} for this call"
diff --git a/quack/gemm_runtime/identity.py b/quack/gemm_runtime/identity.py
index 6427d45c45c88b4d926f5a05f56c71c82c48198e..39a23cb67a6705dd727093690e7eb64e332b7a62 100644
--- a/quack/gemm_runtime/identity.py
+++ b/quack/gemm_runtime/identity.py
@@ -122,0 +123,7 @@ def semantic_value_key(value, seen, *, force_source=False):
+        if force_source and not (
+            value.__module__ == "quack" or value.__module__.startswith("quack.")
+        ):
+            raise TypeError(
+                f"cannot fingerprint callable class {value!r}; use a function wrapper "
+                "or a callable instance implementing __quack_semantic_key__"
+            )
@@ -124,0 +132,6 @@ def semantic_value_key(value, seen, *, force_source=False):
+        if callable(value) and not (
+            type(value).__module__ == "quack" or type(value).__module__.startswith("quack.")
+        ):
+            raise TypeError(
+                f"cannot fingerprint callable dataclass {value!r}; implement __quack_semantic_key__"
+            )
diff --git a/quack/grouped_reduce.py b/quack/grouped_reduce.py
index 63282b4092a863a0395946a3bfebac82c8b0a9a1..a2847ca79231334f49aebf7deedb931412f695b7 100644
--- a/quack/grouped_reduce.py
+++ b/quack/grouped_reduce.py
@@ -514,0 +515,5 @@ class GroupedReduceBase(EpiOp):
+    ``reduce_planes`` is the fixed number of Float32 state components. For more
+    than one plane, the sink returns a tuple and callable combine/finalize
+    receive the complete state tuple. ``fragment_reduced`` means each returned
+    plane is already reduced and broadcast within its register fragment; the op
+    skips only that local fold and still combines subtiles, lanes, and warps.
@@ -526,0 +532,2 @@ class GroupedReduceBase(EpiOp):
+        reduce_planes=1,
+        fragment_reduced=False,
@@ -539,0 +547,8 @@ class GroupedReduceBase(EpiOp):
+        if not isinstance(reduce_planes, int) or reduce_planes < 1:
+            raise ValueError("reduce_planes must be a positive integer")
+        if reduce_planes > 1 and (not callable(combine) or not callable(finalize)):
+            raise TypeError("multi-plane grouped reductions require callable combine and finalize")
+        if not isinstance(fragment_reduced, bool):
+            raise TypeError("fragment_reduced must be bool")
+        if fragment_reduced and combine is None:
+            raise ValueError("fragment_reduced requires a cross-fragment combine")
@@ -543,0 +559,2 @@ class GroupedReduceBase(EpiOp):
+        self.reduce_planes = reduce_planes
+        self.fragment_reduced = fragment_reduced
@@ -547,5 +564,6 @@ class GroupedReduceBase(EpiOp):
-        """Fail-closed static identity: callables fingerprint by source digest
-        (``_callable_config_key``), so two epilogues with different generated
-        combine/finalize code can never share a cached kernel. A subclass that
-        adds configuration must extend this key, exactly like
-        :meth:`EpiOp.config_key` demands of stateful ops."""
+        """Fail-closed identity for geometry, state algebra, and output layout.
+
+        Callable identity includes source, defaults, closures, and referenced
+        globals, so distinct generated state algebra cannot share a kernel.
+        A subclass that adds configuration must extend this key.
+        """
@@ -554 +572,11 @@ class GroupedReduceBase(EpiOp):
-                set(vars(self)) - {"name", "axis", "group", "combine", "finalize", "output_layout"}
+                set(vars(self))
+                - {
+                    "name",
+                    "axis",
+                    "group",
+                    "combine",
+                    "finalize",
+                    "reduce_planes",
+                    "fragment_reduced",
+                    "output_layout",
+                }
@@ -567,0 +596,2 @@ class GroupedReduceBase(EpiOp):
+            self.reduce_planes,
+            self.fragment_reduced,
@@ -595,0 +626,5 @@ class GroupedReduceBase(EpiOp):
+    @property
+    def sink_arity(self):
+        """Number of state planes returned to this sink by the epilogue fn."""
+        return self.reduce_planes
+
@@ -600,0 +636,22 @@ class GroupedReduceBase(EpiOp):
+    def _state_planes(self, value):
+        """Return a tuple view of scalar or multi-plane reduction state."""
+        return (value,) if self.reduce_planes == 1 else value
+
+    @cute.jit
+    def _combine_state(self, lhs, rhs):
+        """Combine two tuples of grouped-reduction state components."""
+        if const_expr(self.reduce_planes == 1):
+            return (self.combine_fn(lhs[0], rhs[0]),)
+        result = self.combine_fn(lhs, rhs)
+        assert isinstance(result, tuple) and len(result) == self.reduce_planes, (
+            f"combine must return {self.reduce_planes} state planes"
+        )
+        return result
+
+    @cute.jit
+    def _finalize_state(self, values):
+        """Project grouped-reduction state to the scalar output value."""
+        if const_expr(self.reduce_planes == 1):
+            return self.finalize_value(values[0])
+        return self.finalize(values)
+
@@ -761 +818,3 @@ class GroupedReduceBase(EpiOp):
-        return EpiSmemBytes(unstaged=cta_tile_shape_mnk[1] * planes * (Float32.width // 8))
+        return EpiSmemBytes(
+            unstaged=cta_tile_shape_mnk[1] * planes * self.reduce_planes * (Float32.width // 8)
+        )
@@ -765 +824,4 @@ class GroupedReduceBase(EpiOp):
-        return (gemm.cta_tile_shape_mnk[1], planes) if planes else None
+        if not planes:
+            return None
+        shape = (gemm.cta_tile_shape_mnk[1], planes)
+        return shape if self.reduce_planes == 1 else (*shape, self.reduce_planes)
@@ -771 +833 @@ class GroupedReduceBase(EpiOp):
-        size = shape[0] * shape[1]
+        size = math.prod(shape)
@@ -807 +869,8 @@ class GroupedReduceBase(EpiOp):
-            frag = cute.make_rmem_tensor(shape if keep_subtiles else shape[:3], Float32)
+            frag_shape = shape if keep_subtiles else shape[:3]
+            frag = (
+                cute.make_rmem_tensor(frag_shape, Float32)
+                if const_expr(self.reduce_planes == 1)
+                else tuple(
+                    cute.make_rmem_tensor(frag_shape, Float32) for _ in range(self.reduce_planes)
+                )
+            )
@@ -818 +887 @@ class GroupedReduceBase(EpiOp):
-        """This subtile's slice of the register accumulator."""
+        """This subtile's slice of each register-state plane."""
@@ -821 +890,5 @@ class GroupedReduceBase(EpiOp):
-        if const_expr(cute.rank(state.frag) == 3):
+        if const_expr(self.reduce_planes == 1):
+            if const_expr(cute.rank(state.frag) == 3):
+                return state.frag
+            return state.frag[None, None, None, epi_coord[0], epi_coord[1]]
+        if const_expr(cute.rank(state.frag[0]) == 3):
@@ -823 +896 @@ class GroupedReduceBase(EpiOp):
-        return state.frag[None, None, None, epi_coord[0], epi_coord[1]]
+        return tuple(plane[None, None, None, epi_coord[0], epi_coord[1]] for plane in state.frag)
@@ -928,0 +1002 @@ class GroupedReduceBase(EpiOp):
+        value_planes = self._state_planes(values)
@@ -939 +1013,4 @@ class GroupedReduceBase(EpiOp):
-                value = self.finalize_value(values[i]) if const_expr(finalize) else values[i]
+                state_value = tuple(plane[i] for plane in value_planes)
+                value = (
+                    self._finalize_state(state_value) if const_expr(finalize) else state_value[0]
+                )
@@ -954,3 +1031,3 @@ class GroupedLocalReduce(GroupedReduceBase):
-    ``combine=None`` skips the fold: the values are expected to be group-reduced
-    and broadcast already (what FlexGEMM's generated TensorSSA does for N groups
-    inside one fragment, and what :class:`GroupedLocalReduceFeed` produces).
+    ``combine=None`` skips every fold because values are fully group-reduced.
+    ``fragment_reduced=True`` skips only the in-fragment fold because TensorSSA
+    or another producer already reduced and broadcast each fragment partial.
@@ -963,4 +1040,6 @@ class GroupedLocalReduce(GroupedReduceBase):
-    def fn_sink_flush(self, gemm, state, frag):
-        """Collect the fn's values for this subtile; the fold runs in end_loop
-        (one reduction site, with the coordinates the store needs)."""
-        cute.autovec_copy(frag, state.frag)
+    def fn_sink_flush(self, gemm, state, *fragments):
+        """Collect each state plane; the physical fold runs in end_loop."""
+        assert len(fragments) == self.reduce_planes
+        destinations = self._state_planes(state.frag)
+        for source, destination in zip(fragments, destinations):
+            cute.autovec_copy(source, destination)
@@ -969,7 +1048,6 @@ class GroupedLocalReduce(GroupedReduceBase):
-    def _fold_fragment(self, frag, geom):
-        """Fold each aligned column run in place, broadcast within the run.
-        Left-to-right so the tree is fixed and reproducible."""
-        combine_fn = const_expr(self.combine_fn)
-        for chunk in cutlass.range_constexpr(len(geom.chunks)):
-            slots = const_expr(geom.chunks[chunk])
-            value = frag[slots[0]]
+    def _fold_chunks(self, frag, chunks):
+        """Fold each static state chunk in place and broadcast its result."""
+        planes = self._state_planes(frag)
+        for chunk in cutlass.range_constexpr(len(chunks)):
+            slots = const_expr(chunks[chunk])
+            values = tuple(plane[slots[0]] for plane in planes)
@@ -977 +1055 @@ class GroupedLocalReduce(GroupedReduceBase):
-                value = combine_fn(value, frag[slots[j]])
+                values = self._combine_state(values, tuple(plane[slots[j]] for plane in planes))
@@ -979,13 +1057,2 @@ class GroupedLocalReduce(GroupedReduceBase):
-                frag[slots[j]] = value
-
-    @cute.jit
-    def _fold_rows(self, frag, geom):
-        """Combine rows one thread owns inside each logical M group."""
-        combine_fn = const_expr(self.combine_fn)
-        for chunk in cutlass.range_constexpr(len(geom.row_chunks)):
-            slots = const_expr(geom.row_chunks[chunk])
-            value = frag[slots[0]]
-            for j in cutlass.range_constexpr(1, len(slots)):
-                value = combine_fn(value, frag[slots[j]])
-            for j in cutlass.range_constexpr(len(slots)):
-                frag[slots[j]] = value
+                for plane, value in zip(planes, values):
+                    plane[slots[j]] = value
@@ -995,3 +1062,2 @@ class GroupedLocalReduce(GroupedReduceBase):
-        """Reduce across the group's row lanes (halving butterfly), leaving the
-        group value in every participating lane."""
-        combine_fn = const_expr(self.combine_fn)
+        """Reduce across the group's row lanes and broadcast each state plane."""
+        planes = self._state_planes(frag)
@@ -999 +1065,2 @@ class GroupedLocalReduce(GroupedReduceBase):
-        for i in cutlass.range(cute.size(frag), unroll_full=True):
+        for i in cutlass.range(cute.size(planes[0]), unroll_full=True):
+            values = tuple(plane[i] for plane in planes)
@@ -1002,5 +1069,4 @@ class GroupedLocalReduce(GroupedReduceBase):
-                frag[i] = combine_fn(
-                    frag[i],
-                    cute.arch.shuffle_sync_bfly(
-                        frag[i], offset=cute.crd2idx((rows, 0), geom.lane_layout_MN)
-                    ),
+                offset = cute.crd2idx((rows, 0), geom.lane_layout_MN)
+                values = self._combine_state(
+                    values,
+                    tuple(cute.arch.shuffle_sync_bfly(value, offset=offset) for value in values),
@@ -1008,0 +1075,2 @@ class GroupedLocalReduce(GroupedReduceBase):
+            for plane, value in zip(planes, values):
+                plane[i] = value
@@ -1012,3 +1080 @@ class GroupedLocalReduce(GroupedReduceBase):
-        """Combine the ``fragments_per_group`` epi-N subtiles of one group in
-        ascending order (called at the group's last subtile)."""
-        combine_fn = const_expr(self.combine_fn)
+        """Combine consecutive epi-N subtiles into one grouped state."""
@@ -1016,2 +1082,4 @@ class GroupedLocalReduce(GroupedReduceBase):
-        merged = cute.make_rmem_tensor_like(
-            state.frag[None, None, None, epi_coord[0], first], Float32
+        state_planes = self._state_planes(state.frag)
+        merged_planes = tuple(
+            cute.make_rmem_tensor_like(plane[None, None, None, epi_coord[0], first], Float32)
+            for plane in state_planes
@@ -1019,2 +1087,3 @@ class GroupedLocalReduce(GroupedReduceBase):
-        cute.autovec_copy(state.frag[None, None, None, epi_coord[0], first], merged)
-        merged_flt = cute.filter_zeros(merged)
+        for source, merged in zip(state_planes, merged_planes):
+            cute.autovec_copy(source[None, None, None, epi_coord[0], first], merged)
+        merged_planes = tuple(cute.filter_zeros(plane) for plane in merged_planes)
@@ -1022,4 +1091,12 @@ class GroupedLocalReduce(GroupedReduceBase):
-            other = cute.filter_zeros(state.frag[None, None, None, epi_coord[0], first + offset])
-            for i in cutlass.range(cute.size(merged_flt), unroll_full=True):
-                merged_flt[i] = combine_fn(merged_flt[i], other[i])
-        return merged_flt
+            other_planes = tuple(
+                cute.filter_zeros(plane[None, None, None, epi_coord[0], first + offset])
+                for plane in state_planes
+            )
+            for i in cutlass.range(cute.size(merged_planes[0]), unroll_full=True):
+                values = self._combine_state(
+                    tuple(plane[i] for plane in merged_planes),
+                    tuple(plane[i] for plane in other_planes),
+                )
+                for plane, value in zip(merged_planes, values):
+                    plane[i] = value
+        return merged_planes[0] if const_expr(self.reduce_planes == 1) else merged_planes
@@ -1034 +1111 @@ class GroupedLocalReduce(GroupedReduceBase):
-        combine_fn = const_expr(self.combine_fn)
+        planes = self._state_planes(frag)
@@ -1036 +1113 @@ class GroupedLocalReduce(GroupedReduceBase):
-        for i in cutlass.range(cute.size(frag), unroll_full=True):
+        for i in cutlass.range(cute.size(planes[0]), unroll_full=True):
@@ -1041 +1118,6 @@ class GroupedLocalReduce(GroupedReduceBase):
-                sReduce[n_idx, state.warp_m_idx - group_idx - 1] = frag[i]
+                smem_warp = state.warp_m_idx - group_idx - 1
+                for plane_idx, plane in enumerate(planes):
+                    if const_expr(self.reduce_planes == 1):
+                        sReduce[n_idx, smem_warp] = plane[i]
+                    else:
+                        sReduce[n_idx, smem_warp, plane_idx] = plane[i]
@@ -1043 +1125 @@ class GroupedLocalReduce(GroupedReduceBase):
-        for i in cutlass.range(cute.size(frag), unroll_full=True):
+        for i in cutlass.range(cute.size(planes[0]), unroll_full=True):
@@ -1046,0 +1129 @@ class GroupedLocalReduce(GroupedReduceBase):
+                values = tuple(plane[i] for plane in planes)
@@ -1048,3 +1131,6 @@ class GroupedLocalReduce(GroupedReduceBase):
-                    frag[i] = combine_fn(
-                        frag[i],
-                        sReduce[n_idx, group_idx * geom.group_warps + offset - group_idx - 1],
+                    smem_warp = group_idx * geom.group_warps + offset - group_idx - 1
+                    others = tuple(
+                        sReduce[n_idx, smem_warp]
+                        if const_expr(self.reduce_planes == 1)
+                        else sReduce[n_idx, smem_warp, plane_idx]
+                        for plane_idx in range(self.reduce_planes)
@@ -1051,0 +1138,3 @@ class GroupedLocalReduce(GroupedReduceBase):
+                    values = self._combine_state(values, others)
+                for plane, value in zip(planes, values):
+                    plane[i] = value
@@ -1071 +1160,6 @@ class GroupedLocalReduce(GroupedReduceBase):
-        frag = cute.filter_zeros(self._frag_slice(state, epi_coord))
+        frag = self._frag_slice(state, epi_coord)
+        frag = (
+            cute.filter_zeros(frag)
+            if const_expr(self.reduce_planes == 1)
+            else tuple(cute.filter_zeros(plane) for plane in frag)
+        )
@@ -1074,2 +1168,2 @@ class GroupedLocalReduce(GroupedReduceBase):
-                if const_expr(geom.chunk > 1):
-                    self._fold_fragment(frag, geom)
+                if const_expr(not self.fragment_reduced and geom.chunk > 1):
+                    self._fold_chunks(frag, geom.chunks)
@@ -1082,2 +1176,4 @@ class GroupedLocalReduce(GroupedReduceBase):
-                if const_expr(any(len(chunk) > 1 for chunk in geom.row_chunks)):
-                    self._fold_rows(frag, geom)
+                if const_expr(
+                    not self.fragment_reduced and any(len(chunk) > 1 for chunk in geom.row_chunks)
+                ):
+                    self._fold_chunks(frag, geom.row_chunks)
@@ -1208 +1304 @@ class GroupedLocalReduceWithFinalizeArg(GroupedLocalReduce):
-    scaled = True
+    sink_arity = 2
@@ -1256,5 +1352,5 @@ class GroupedLocalReduceWithFinalizeArg(GroupedLocalReduce):
-    def fn_sink_flush(self, gemm, state, frag, scale=None):
-        """Capture the sum source and prepass value emitted as a sink pair."""
-        assert scale is not None
-        cute.autovec_copy(frag, state.frag)
-        cute.autovec_copy(scale, state.finalize_arg)
+    def fn_sink_flush(self, gemm, state, *fragments):
+        """Capture one reduction plane and one independent finalizer argument."""
+        assert len(fragments) == 2
+        cute.autovec_copy(fragments[0], state.frag)
+        cute.autovec_copy(fragments[1], state.finalize_arg)
@@ -1290 +1386 @@ class GroupedLocalReduceWithFinalizeArg(GroupedLocalReduce):
-            self._fold_fragment(frag, geom)
+            self._fold_chunks(frag, geom.chunks)
