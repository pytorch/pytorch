# Mapping (to ATen) function signatures. These are exposed in Python, but do not have a
# derivative specified (because they are implemented in terms of differentiable ATen functions).

- name: split(Tensor self, int64_t split_size, int64_t dim=0)

- name: chunk(Tensor self, int64_t chunks, int64_t dim=0)
