[project]
name = "torch-ci"
version = "0.1.0"
dependencies = [
    "cement>=3.0.0",
    "pyyaml>=6.0"
]

[tool.setuptools]
packages = ["cli", "cli.build", "cli.test", "cli.lib"]

[tool.setuptools.package-dir]
cli = "cli"
