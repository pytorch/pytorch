name: Linux ROCm PyTorch build and test

on:
  push:
    tags:
      - ciflow/rocm/*
    branches:
      - main

permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

jobs:
  # Compile torch (with this Kineto) on an AWS CPU runner so sccache can reach
  # S3. gpu-arch-type must stay cpu: the rocm setup action fails when rocminfo
  # finds no GPU. The wheel is uploaded as a GitHub Actions artifact, same
  # mechanism as linux_rocm_kineto.yml but a different artifact name.
  build:
    strategy:
      matrix:
        rocm: ["10.0"]
    uses: pytorch/test-infra/.github/workflows/linux_job_v2.yml@main
    with:
      job-name: linux-rocm-${{ matrix.rocm }}-pytorch-build
      runner: linux.12xlarge
      timeout: 180
      repository: ${{ github.repository }}
      ref: ${{ github.ref }}
      submodules: recursive
      gpu-arch-type: cpu
      # gpu-arch-type stays cpu so the ROCm setup action is not run (it fails
      # with no GPU). docker-image still has to be explicit for that reason.
      gpu-arch-version: ${{ matrix.rocm }}
      docker-image: pytorch/almalinux-builder:rocm${{ matrix.rocm }}
      upload-artifact: linux-rocm-pytorch-wheel
      script: |
        set -eux
        .github/scripts/setup.sh
        .github/scripts/pytorch_build_test.sh rocm wheel

  test:
    needs: build
    strategy:
      matrix:
        rocm: ["10.0"]
    uses: pytorch/test-infra/.github/workflows/linux_job_v2.yml@main
    with:
      job-name: linux-rocm-${{ matrix.rocm }}-pytorch-test
      runner: linux.rocm.gpu.ecosystem.mi350.1
      timeout: 180
      repository: ${{ github.repository }}
      ref: ${{ github.ref }}
      submodules: recursive
      gpu-arch-type: rocm
      gpu-arch-version: ${{ matrix.rocm }}
      download-artifact: linux-rocm-pytorch-wheel
      script: |
        set -eux
        .github/scripts/setup.sh
        .github/scripts/pytorch_build_test.sh rocm test
