# Workflow: Dynamo Unit Test
# runs unit tests for dynamo.
name: dynamo-unittest

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

jobs:
  get-label-type:
    name: get-label-type
    uses: pytorch/pytorch/.github/workflows/_runner-determinator.yml@main
    if: ${{ (github.event_name != 'schedule' || github.repository == 'pytorch/pytorch') && github.repository_owner == 'pytorch' }}
    with:
      triggering_actor: ${{ github.triggering_actor }}
      issue_owner: ${{ github.event.pull_request.user.login || github.event.issue.user.login }}
      curr_branch: ${{ github.head_ref || github.ref_name }}
      curr_ref_type: ${{ github.ref_type }}
      opt_out_experiments: lf

  dynamo-build:
    name: dynamo-cpython-build
    uses: ./.github/workflows/_linux-build.yml
    needs: get-label-type
    strategy:
      matrix:
        python-version: ['3.13']
    with:
      runner_prefix: "${{ needs.get-label-type.outputs.label-type }}"
      build-environment: linux-jammy-py${{ matrix.python-version }}-clang15
      docker-image-name: ci-image:pytorch-linux-jammy-py${{ matrix.python-version }}-clang15
      test-matrix: |
        { include: [
          { config: "dynamo_cpython", shard: 1, num_shards: 1, runner: "${{ needs.get-label-type.outputs.label-type }}linux.c7i.2xlarge" },
        ]}
    secrets: inherit

  dynamo-test:
    name: dynamo-cpython-test
    uses: ./.github/workflows/_linux-test.yml
    needs: [get-label-type, dynamo-cpython-build]
    strategy:
      matrix:
        python-version: ['3.13']
    with:
      build-environment: linux-jammy-py${{ matrix.python-version }}-clang15
      docker-image: ci-image:pytorch-linux-jammy-py${{ matrix.python-version }}-clang15
      test-matrix: |
        { include: [
          { config: "dynamo_cpython", shard: 1, num_shards: 1, runner: "${{ needs.get-label-type.outputs.label-type }}linux.c7i.2xlarge" },
        ]}
    secrets: inherit
